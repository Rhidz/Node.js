const hobbies = ["Sports"," Music", "Fitness"]
console.log(hobbies.map(hobby => "Hobby: " + hobby)) // for every element, hobby execute the call back func denoted by the arrow func
console.log(hobbies)